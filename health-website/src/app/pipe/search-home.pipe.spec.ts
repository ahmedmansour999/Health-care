import { SearchHomePipe } from './search-home.pipe';

describe('SearchHomePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchHomePipe();
    expect(pipe).toBeTruthy();
  });
});
